# OpenGL-Template
A template for modern OpenGL

# Setup
Run the following in the project root to build the project

`mkdir build`

`cmake ..`

`make -j$(nprocs)`

# Running the project

`./build/main`

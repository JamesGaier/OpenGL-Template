# OpenGL-Template
A template for modern OpenGL

## Setup
Run the following in the project root to build the project with `make`:
```
mkdir build
cd build
cmake ..
make -j$(nprocs)
```

## Running the project
`./build/main`
